export default {
    /*created: function(el, binding) {
        //console.log(el, binding);
        el.style.background = binding.value;
    } a sintaxe fica assim se for usado direto em um componente */
    created(el, binding) {
        //console.log(el, binding);
        el.style.background = binding.value;
    }
}